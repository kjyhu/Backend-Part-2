/**The different programs communicate with each to make changes
 * in the backend of my music app because they can modify the app
 * without actually having to open the file itself instead, you can 
 * retrive the link on backend and make the changes on the backend.
 * Get is the basic of the page where it is what the user see on the
 * page. Post adds an course to the courses array and is being displayed
 * throguh get. Put updates a course that's in the courses array based on
 * the id. Delete deletes a course in the courses array based on the id
 * and returnhs an error if the id doesn't exist 
 */